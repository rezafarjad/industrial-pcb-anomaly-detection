# Generated model artifacts

Run notebook Phases 2-4 to create:

```text
artifacts/
├── pcb1/{config.json,patchcore_memory.npy,knn_memory.npy,pca.joblib}
├── pcb2/{config.json,patchcore_memory.npy,knn_memory.npy,pca.joblib}
└── pcb3/{config.json,patchcore_memory.npy,knn_memory.npy,pca.joblib}
```

Commit the PCA, PatchCore memory, and configuration files so the
Streamlit application can run after cloning the repository.
